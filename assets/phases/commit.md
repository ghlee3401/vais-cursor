### 커밋 생성

1. `git diff --staged`로 변경사항 확인
2. Conventional Commits 형식으로 메시지 생성:
   ```
   feat(auth): 로그인 API 엔드포인트 추가
   fix(ui): 모바일 레이아웃 깨짐 수정
   ```
3. convention의 커밋 컨벤션 참조
4. AskUserQuestion으로 메시지 확인 후 커밋
